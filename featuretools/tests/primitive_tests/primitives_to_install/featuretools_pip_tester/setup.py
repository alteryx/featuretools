from setuptools import setup

setup(name='featuretools-pip-tester',
      version='0.1',
      description='Module to test if the featuretools install commmand will '\
                  'install pip modules',
      packages=['featuretools_pip_tester'])
